-----------------------------------------------------------------------------------------------------------------------
--                                      Application aliases for tasklist widget                                      --
-----------------------------------------------------------------------------------------------------------------------

-- this replaces the name shown in the tasklist with the specified alternative
-- this is useful when the default name is somehow unfitting / too long / not what is expected

local appnames = {}

appnames["Vivaldi-stable"      ] = "Vivaldi"
appnames["code-oss"            ] = "VSCode"
appnames["Qemu-system-x86_64"  ] = "qemu"

return appnames

